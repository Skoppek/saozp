config:
  target: "http://localhost:2358"
  phases:
    - duration: 60
      arrivalRate: 1
scenarios:
  - flow:
      - log: "logging"
      - get:
          url: "/about"
          headers:
            X-Auth-Token: skopek
